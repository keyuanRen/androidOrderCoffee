<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kyousukeg.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".loginActivity" />
        <activity android:name=".registerActivity" />
        <activity android:name=".shopListActivity" />
        //add back button from products list back to shop list
        <activity
            android:name=".productListActivity"
            android:parentActivityName=".shopListActivity" />
        <activity android:name=".productInformationActivity" />
        <activity android:name=".orderConfirmActivity"></activity>
    </application>

</manifest>